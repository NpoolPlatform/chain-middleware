// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/chain-middleware/pkg/db/ent/fiatcurrencytype"
	"github.com/NpoolPlatform/chain-middleware/pkg/db/ent/predicate"
	"github.com/google/uuid"
)

// FiatCurrencyTypeQuery is the builder for querying FiatCurrencyType entities.
type FiatCurrencyTypeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FiatCurrencyType
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FiatCurrencyTypeQuery builder.
func (fctq *FiatCurrencyTypeQuery) Where(ps ...predicate.FiatCurrencyType) *FiatCurrencyTypeQuery {
	fctq.predicates = append(fctq.predicates, ps...)
	return fctq
}

// Limit adds a limit step to the query.
func (fctq *FiatCurrencyTypeQuery) Limit(limit int) *FiatCurrencyTypeQuery {
	fctq.limit = &limit
	return fctq
}

// Offset adds an offset step to the query.
func (fctq *FiatCurrencyTypeQuery) Offset(offset int) *FiatCurrencyTypeQuery {
	fctq.offset = &offset
	return fctq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fctq *FiatCurrencyTypeQuery) Unique(unique bool) *FiatCurrencyTypeQuery {
	fctq.unique = &unique
	return fctq
}

// Order adds an order step to the query.
func (fctq *FiatCurrencyTypeQuery) Order(o ...OrderFunc) *FiatCurrencyTypeQuery {
	fctq.order = append(fctq.order, o...)
	return fctq
}

// First returns the first FiatCurrencyType entity from the query.
// Returns a *NotFoundError when no FiatCurrencyType was found.
func (fctq *FiatCurrencyTypeQuery) First(ctx context.Context) (*FiatCurrencyType, error) {
	nodes, err := fctq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fiatcurrencytype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) FirstX(ctx context.Context) *FiatCurrencyType {
	node, err := fctq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FiatCurrencyType ID from the query.
// Returns a *NotFoundError when no FiatCurrencyType ID was found.
func (fctq *FiatCurrencyTypeQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = fctq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fiatcurrencytype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := fctq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FiatCurrencyType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FiatCurrencyType entity is found.
// Returns a *NotFoundError when no FiatCurrencyType entities are found.
func (fctq *FiatCurrencyTypeQuery) Only(ctx context.Context) (*FiatCurrencyType, error) {
	nodes, err := fctq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fiatcurrencytype.Label}
	default:
		return nil, &NotSingularError{fiatcurrencytype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) OnlyX(ctx context.Context) *FiatCurrencyType {
	node, err := fctq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FiatCurrencyType ID in the query.
// Returns a *NotSingularError when more than one FiatCurrencyType ID is found.
// Returns a *NotFoundError when no entities are found.
func (fctq *FiatCurrencyTypeQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = fctq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fiatcurrencytype.Label}
	default:
		err = &NotSingularError{fiatcurrencytype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := fctq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FiatCurrencyTypes.
func (fctq *FiatCurrencyTypeQuery) All(ctx context.Context) ([]*FiatCurrencyType, error) {
	if err := fctq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return fctq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) AllX(ctx context.Context) []*FiatCurrencyType {
	nodes, err := fctq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FiatCurrencyType IDs.
func (fctq *FiatCurrencyTypeQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := fctq.Select(fiatcurrencytype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := fctq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fctq *FiatCurrencyTypeQuery) Count(ctx context.Context) (int, error) {
	if err := fctq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return fctq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) CountX(ctx context.Context) int {
	count, err := fctq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fctq *FiatCurrencyTypeQuery) Exist(ctx context.Context) (bool, error) {
	if err := fctq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return fctq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (fctq *FiatCurrencyTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := fctq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FiatCurrencyTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fctq *FiatCurrencyTypeQuery) Clone() *FiatCurrencyTypeQuery {
	if fctq == nil {
		return nil
	}
	return &FiatCurrencyTypeQuery{
		config:     fctq.config,
		limit:      fctq.limit,
		offset:     fctq.offset,
		order:      append([]OrderFunc{}, fctq.order...),
		predicates: append([]predicate.FiatCurrencyType{}, fctq.predicates...),
		// clone intermediate query.
		sql:    fctq.sql.Clone(),
		path:   fctq.path,
		unique: fctq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FiatCurrencyType.Query().
//		GroupBy(fiatcurrencytype.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (fctq *FiatCurrencyTypeQuery) GroupBy(field string, fields ...string) *FiatCurrencyTypeGroupBy {
	grbuild := &FiatCurrencyTypeGroupBy{config: fctq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := fctq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return fctq.sqlQuery(ctx), nil
	}
	grbuild.label = fiatcurrencytype.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.FiatCurrencyType.Query().
//		Select(fiatcurrencytype.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (fctq *FiatCurrencyTypeQuery) Select(fields ...string) *FiatCurrencyTypeSelect {
	fctq.fields = append(fctq.fields, fields...)
	selbuild := &FiatCurrencyTypeSelect{FiatCurrencyTypeQuery: fctq}
	selbuild.label = fiatcurrencytype.Label
	selbuild.flds, selbuild.scan = &fctq.fields, selbuild.Scan
	return selbuild
}

func (fctq *FiatCurrencyTypeQuery) prepareQuery(ctx context.Context) error {
	for _, f := range fctq.fields {
		if !fiatcurrencytype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fctq.path != nil {
		prev, err := fctq.path(ctx)
		if err != nil {
			return err
		}
		fctq.sql = prev
	}
	if fiatcurrencytype.Policy == nil {
		return errors.New("ent: uninitialized fiatcurrencytype.Policy (forgotten import ent/runtime?)")
	}
	if err := fiatcurrencytype.Policy.EvalQuery(ctx, fctq); err != nil {
		return err
	}
	return nil
}

func (fctq *FiatCurrencyTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FiatCurrencyType, error) {
	var (
		nodes = []*FiatCurrencyType{}
		_spec = fctq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FiatCurrencyType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FiatCurrencyType{config: fctq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(fctq.modifiers) > 0 {
		_spec.Modifiers = fctq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fctq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fctq *FiatCurrencyTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fctq.querySpec()
	if len(fctq.modifiers) > 0 {
		_spec.Modifiers = fctq.modifiers
	}
	_spec.Node.Columns = fctq.fields
	if len(fctq.fields) > 0 {
		_spec.Unique = fctq.unique != nil && *fctq.unique
	}
	return sqlgraph.CountNodes(ctx, fctq.driver, _spec)
}

func (fctq *FiatCurrencyTypeQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := fctq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (fctq *FiatCurrencyTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   fiatcurrencytype.Table,
			Columns: fiatcurrencytype.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: fiatcurrencytype.FieldID,
			},
		},
		From:   fctq.sql,
		Unique: true,
	}
	if unique := fctq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := fctq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fiatcurrencytype.FieldID)
		for i := range fields {
			if fields[i] != fiatcurrencytype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fctq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fctq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fctq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fctq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fctq *FiatCurrencyTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fctq.driver.Dialect())
	t1 := builder.Table(fiatcurrencytype.Table)
	columns := fctq.fields
	if len(columns) == 0 {
		columns = fiatcurrencytype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fctq.sql != nil {
		selector = fctq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fctq.unique != nil && *fctq.unique {
		selector.Distinct()
	}
	for _, m := range fctq.modifiers {
		m(selector)
	}
	for _, p := range fctq.predicates {
		p(selector)
	}
	for _, p := range fctq.order {
		p(selector)
	}
	if offset := fctq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fctq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (fctq *FiatCurrencyTypeQuery) ForUpdate(opts ...sql.LockOption) *FiatCurrencyTypeQuery {
	if fctq.driver.Dialect() == dialect.Postgres {
		fctq.Unique(false)
	}
	fctq.modifiers = append(fctq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return fctq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (fctq *FiatCurrencyTypeQuery) ForShare(opts ...sql.LockOption) *FiatCurrencyTypeQuery {
	if fctq.driver.Dialect() == dialect.Postgres {
		fctq.Unique(false)
	}
	fctq.modifiers = append(fctq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return fctq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (fctq *FiatCurrencyTypeQuery) Modify(modifiers ...func(s *sql.Selector)) *FiatCurrencyTypeSelect {
	fctq.modifiers = append(fctq.modifiers, modifiers...)
	return fctq.Select()
}

// FiatCurrencyTypeGroupBy is the group-by builder for FiatCurrencyType entities.
type FiatCurrencyTypeGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fctgb *FiatCurrencyTypeGroupBy) Aggregate(fns ...AggregateFunc) *FiatCurrencyTypeGroupBy {
	fctgb.fns = append(fctgb.fns, fns...)
	return fctgb
}

// Scan applies the group-by query and scans the result into the given value.
func (fctgb *FiatCurrencyTypeGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := fctgb.path(ctx)
	if err != nil {
		return err
	}
	fctgb.sql = query
	return fctgb.sqlScan(ctx, v)
}

func (fctgb *FiatCurrencyTypeGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range fctgb.fields {
		if !fiatcurrencytype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := fctgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fctgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (fctgb *FiatCurrencyTypeGroupBy) sqlQuery() *sql.Selector {
	selector := fctgb.sql.Select()
	aggregation := make([]string, 0, len(fctgb.fns))
	for _, fn := range fctgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(fctgb.fields)+len(fctgb.fns))
		for _, f := range fctgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(fctgb.fields...)...)
}

// FiatCurrencyTypeSelect is the builder for selecting fields of FiatCurrencyType entities.
type FiatCurrencyTypeSelect struct {
	*FiatCurrencyTypeQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (fcts *FiatCurrencyTypeSelect) Scan(ctx context.Context, v interface{}) error {
	if err := fcts.prepareQuery(ctx); err != nil {
		return err
	}
	fcts.sql = fcts.FiatCurrencyTypeQuery.sqlQuery(ctx)
	return fcts.sqlScan(ctx, v)
}

func (fcts *FiatCurrencyTypeSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := fcts.sql.Query()
	if err := fcts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (fcts *FiatCurrencyTypeSelect) Modify(modifiers ...func(s *sql.Selector)) *FiatCurrencyTypeSelect {
	fcts.modifiers = append(fcts.modifiers, modifiers...)
	return fcts
}
