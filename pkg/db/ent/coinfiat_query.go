// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/chain-middleware/pkg/db/ent/coinfiat"
	"github.com/NpoolPlatform/chain-middleware/pkg/db/ent/predicate"
)

// CoinFiatQuery is the builder for querying CoinFiat entities.
type CoinFiatQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CoinFiat
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CoinFiatQuery builder.
func (cfq *CoinFiatQuery) Where(ps ...predicate.CoinFiat) *CoinFiatQuery {
	cfq.predicates = append(cfq.predicates, ps...)
	return cfq
}

// Limit adds a limit step to the query.
func (cfq *CoinFiatQuery) Limit(limit int) *CoinFiatQuery {
	cfq.limit = &limit
	return cfq
}

// Offset adds an offset step to the query.
func (cfq *CoinFiatQuery) Offset(offset int) *CoinFiatQuery {
	cfq.offset = &offset
	return cfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cfq *CoinFiatQuery) Unique(unique bool) *CoinFiatQuery {
	cfq.unique = &unique
	return cfq
}

// Order adds an order step to the query.
func (cfq *CoinFiatQuery) Order(o ...OrderFunc) *CoinFiatQuery {
	cfq.order = append(cfq.order, o...)
	return cfq
}

// First returns the first CoinFiat entity from the query.
// Returns a *NotFoundError when no CoinFiat was found.
func (cfq *CoinFiatQuery) First(ctx context.Context) (*CoinFiat, error) {
	nodes, err := cfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{coinfiat.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfq *CoinFiatQuery) FirstX(ctx context.Context) *CoinFiat {
	node, err := cfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CoinFiat ID from the query.
// Returns a *NotFoundError when no CoinFiat ID was found.
func (cfq *CoinFiatQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{coinfiat.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfq *CoinFiatQuery) FirstIDX(ctx context.Context) int {
	id, err := cfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CoinFiat entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CoinFiat entity is found.
// Returns a *NotFoundError when no CoinFiat entities are found.
func (cfq *CoinFiatQuery) Only(ctx context.Context) (*CoinFiat, error) {
	nodes, err := cfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{coinfiat.Label}
	default:
		return nil, &NotSingularError{coinfiat.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfq *CoinFiatQuery) OnlyX(ctx context.Context) *CoinFiat {
	node, err := cfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CoinFiat ID in the query.
// Returns a *NotSingularError when more than one CoinFiat ID is found.
// Returns a *NotFoundError when no entities are found.
func (cfq *CoinFiatQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{coinfiat.Label}
	default:
		err = &NotSingularError{coinfiat.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfq *CoinFiatQuery) OnlyIDX(ctx context.Context) int {
	id, err := cfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CoinFiats.
func (cfq *CoinFiatQuery) All(ctx context.Context) ([]*CoinFiat, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cfq *CoinFiatQuery) AllX(ctx context.Context) []*CoinFiat {
	nodes, err := cfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CoinFiat IDs.
func (cfq *CoinFiatQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cfq.Select(coinfiat.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfq *CoinFiatQuery) IDsX(ctx context.Context) []int {
	ids, err := cfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfq *CoinFiatQuery) Count(ctx context.Context) (int, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cfq *CoinFiatQuery) CountX(ctx context.Context) int {
	count, err := cfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfq *CoinFiatQuery) Exist(ctx context.Context) (bool, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cfq *CoinFiatQuery) ExistX(ctx context.Context) bool {
	exist, err := cfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CoinFiatQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfq *CoinFiatQuery) Clone() *CoinFiatQuery {
	if cfq == nil {
		return nil
	}
	return &CoinFiatQuery{
		config:     cfq.config,
		limit:      cfq.limit,
		offset:     cfq.offset,
		order:      append([]OrderFunc{}, cfq.order...),
		predicates: append([]predicate.CoinFiat{}, cfq.predicates...),
		// clone intermediate query.
		sql:    cfq.sql.Clone(),
		path:   cfq.path,
		unique: cfq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CoinFiat.Query().
//		GroupBy(coinfiat.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (cfq *CoinFiatQuery) GroupBy(field string, fields ...string) *CoinFiatGroupBy {
	grbuild := &CoinFiatGroupBy{config: cfq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cfq.sqlQuery(ctx), nil
	}
	grbuild.label = coinfiat.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.CoinFiat.Query().
//		Select(coinfiat.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (cfq *CoinFiatQuery) Select(fields ...string) *CoinFiatSelect {
	cfq.fields = append(cfq.fields, fields...)
	selbuild := &CoinFiatSelect{CoinFiatQuery: cfq}
	selbuild.label = coinfiat.Label
	selbuild.flds, selbuild.scan = &cfq.fields, selbuild.Scan
	return selbuild
}

func (cfq *CoinFiatQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cfq.fields {
		if !coinfiat.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cfq.path != nil {
		prev, err := cfq.path(ctx)
		if err != nil {
			return err
		}
		cfq.sql = prev
	}
	if coinfiat.Policy == nil {
		return errors.New("ent: uninitialized coinfiat.Policy (forgotten import ent/runtime?)")
	}
	if err := coinfiat.Policy.EvalQuery(ctx, cfq); err != nil {
		return err
	}
	return nil
}

func (cfq *CoinFiatQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CoinFiat, error) {
	var (
		nodes = []*CoinFiat{}
		_spec = cfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CoinFiat).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CoinFiat{config: cfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(cfq.modifiers) > 0 {
		_spec.Modifiers = cfq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cfq *CoinFiatQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfq.querySpec()
	if len(cfq.modifiers) > 0 {
		_spec.Modifiers = cfq.modifiers
	}
	_spec.Node.Columns = cfq.fields
	if len(cfq.fields) > 0 {
		_spec.Unique = cfq.unique != nil && *cfq.unique
	}
	return sqlgraph.CountNodes(ctx, cfq.driver, _spec)
}

func (cfq *CoinFiatQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cfq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (cfq *CoinFiatQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   coinfiat.Table,
			Columns: coinfiat.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: coinfiat.FieldID,
			},
		},
		From:   cfq.sql,
		Unique: true,
	}
	if unique := cfq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cfq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, coinfiat.FieldID)
		for i := range fields {
			if fields[i] != coinfiat.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cfq *CoinFiatQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cfq.driver.Dialect())
	t1 := builder.Table(coinfiat.Table)
	columns := cfq.fields
	if len(columns) == 0 {
		columns = coinfiat.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cfq.sql != nil {
		selector = cfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cfq.unique != nil && *cfq.unique {
		selector.Distinct()
	}
	for _, m := range cfq.modifiers {
		m(selector)
	}
	for _, p := range cfq.predicates {
		p(selector)
	}
	for _, p := range cfq.order {
		p(selector)
	}
	if offset := cfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (cfq *CoinFiatQuery) ForUpdate(opts ...sql.LockOption) *CoinFiatQuery {
	if cfq.driver.Dialect() == dialect.Postgres {
		cfq.Unique(false)
	}
	cfq.modifiers = append(cfq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return cfq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (cfq *CoinFiatQuery) ForShare(opts ...sql.LockOption) *CoinFiatQuery {
	if cfq.driver.Dialect() == dialect.Postgres {
		cfq.Unique(false)
	}
	cfq.modifiers = append(cfq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return cfq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cfq *CoinFiatQuery) Modify(modifiers ...func(s *sql.Selector)) *CoinFiatSelect {
	cfq.modifiers = append(cfq.modifiers, modifiers...)
	return cfq.Select()
}

// CoinFiatGroupBy is the group-by builder for CoinFiat entities.
type CoinFiatGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfgb *CoinFiatGroupBy) Aggregate(fns ...AggregateFunc) *CoinFiatGroupBy {
	cfgb.fns = append(cfgb.fns, fns...)
	return cfgb
}

// Scan applies the group-by query and scans the result into the given value.
func (cfgb *CoinFiatGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cfgb.path(ctx)
	if err != nil {
		return err
	}
	cfgb.sql = query
	return cfgb.sqlScan(ctx, v)
}

func (cfgb *CoinFiatGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cfgb.fields {
		if !coinfiat.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cfgb *CoinFiatGroupBy) sqlQuery() *sql.Selector {
	selector := cfgb.sql.Select()
	aggregation := make([]string, 0, len(cfgb.fns))
	for _, fn := range cfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cfgb.fields)+len(cfgb.fns))
		for _, f := range cfgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cfgb.fields...)...)
}

// CoinFiatSelect is the builder for selecting fields of CoinFiat entities.
type CoinFiatSelect struct {
	*CoinFiatQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cfs *CoinFiatSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cfs.prepareQuery(ctx); err != nil {
		return err
	}
	cfs.sql = cfs.CoinFiatQuery.sqlQuery(ctx)
	return cfs.sqlScan(ctx, v)
}

func (cfs *CoinFiatSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cfs.sql.Query()
	if err := cfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cfs *CoinFiatSelect) Modify(modifiers ...func(s *sql.Selector)) *CoinFiatSelect {
	cfs.modifiers = append(cfs.modifiers, modifiers...)
	return cfs
}
